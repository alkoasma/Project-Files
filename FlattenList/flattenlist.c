SLnodePtr flattenList (const HBnodePtr L){
    int i;
    struct node{
        int value;
        struct node *next;
        struct node *bottom; // Check if required.
    }
    
    int a[];

    struct node *first = Null;

    while(new_node -> value != NULL)
        a++ = new_node -> value;
        first = new_node -> bottom;
        while (first -> value != NULL)
            a++ == first -> value;
            first = first -> bottom;

        new_node = new_node -> next; 
}
